import 'package:flutter/material.dart';

class NotifyBody extends StatelessWidget {
  const NotifyBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('1')),
    );
  }
}
